#include <iostream>

#include <psm/controller.hpp>
#include <psm/plant.hpp>

int main() {
    // TODO (Misja 6: pierwszy_przebieg):
    // 1) stwórz Plant, dodaj jedną paczkę przez spawnItem (wybierz dowolną masę),
    // 2) w pętli (np. 6 ticków): policz WeightClass i DiverterPosition tak jak w runTicks,
    //    wywołaj advance(plant, ...), i wypisz na konsolę numer ticku oraz aktualną strefę
    //    (albo "empty", jeśli plant.item nie ma wartości) -- użyj psm::toString(zone).
    return 0;
}
